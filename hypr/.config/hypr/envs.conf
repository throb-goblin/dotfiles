# Extra env variables
# Note: You must relaunch Hyprland after changing envs (use Super+Esc, then Relaunch)
# env = MY_GLOBAL_ENV,setting

# Qt HiDPI: let Qt auto-detect scale from the Wayland compositor
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
